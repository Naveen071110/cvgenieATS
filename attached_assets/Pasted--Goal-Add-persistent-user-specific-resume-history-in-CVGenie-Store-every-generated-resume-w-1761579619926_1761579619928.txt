**Goal:**  
Add persistent, user-specific resume history in CVGenie. Store every generated resume with timestamp for each Clerk-authenticated user in Neon Postgres, and add a Resume History API and UI so users can view/download their past resumes anytime. No previous resume history section exists—add this as a new dashboard view. Code must be production-ready, with env setup, clean comments, and error-proof for Replit.

### **Requirements**

1. **Dependencies & Environment**
   - Show how to install Neon (+pg or @neondatabase/serverless if better on Replit), Express, Clerk SDK for auth.
   - Provide a sample `.env` file including NEON_CONNECTION_STRING.

2. **Database Setup**
   - Table: `resumes`
     - id SERIAL PRIMARY KEY
     - user_id VARCHAR(255) NOT NULL
     - resume_data TEXT NOT NULL
     - created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP

3. **DB Connection (TypeScript)**
   - Use clean code and async patterns.
   - Connection pooling if available for Replit/Neon.

4. **Resume Storage Logic**
   - Write `insertResume(userId: string, resumeData: string): Promise<void>` that writes a row with error handling/logging.
   - Write `getResumesByUser(userId: string): Promise<Resume[]>` to fetch all resumes for a user, ordered by newest first.

5. **Express API Endpoints**
   - POST `/api/resume` – Authenticated route, saves resume for logged-in Clerk user.
   - GET `/api/resume-history` – Authenticated route, returns all resumes for current user in reverse date order, JSON array with id, data, date.
   - Always validate Clerk session and extract user_id from token/request.

6. **Frontend Update Guidance**
   - Add a “Resume History” section/page in dashboard.
   - Frontend (React/JS): On load, fetch `/api/resume-history`; display a list with resume preview, created date, and button to view/download each.
   - Show helpful message if empty ("No resumes yet!").
   - Optionally, after each generation/refetch, show updated history instantly.

7. **Comments & Error Handling**
   - Explain every block with concise code comments.
   - All API and DB ops have try/catch; return helpful errors for 4xx/5xx or missing/resume not found.

8. **End-to-End Instructions**
   - Copy-paste ready install commands.
   - `.env` sample with placeholders, one-line description for each var.
   - Step-by-step for database, backend, and frontend integration.

***

### **Checklist for Agent/Assistant**
- All steps and code should work out-of-box in Replit cloud environment
- Node.js/TypeScript native best practices
- Only use public, free (or project-included) packages
- Auth must be connected to Clerk so users only see their data
- Code well-sectioned for fast future upgrades (batch delete, renaming histories, etc.)
- [Prompt based on Neon code sample, Express, Clerk auth, and Replit file patterns]

***

**Deliver:**
- Yarn/NPM commands
- `.env` setup
- SQL schema/create command
- Full TypeScript backend code (Neon connect, insert, fetch, API handlers)
- Brief sample React/JS endpoint call for Resume History
- Make sure the “Resume History” dashboard section is part of the prompt

***

**Target:**  
A real, working resume history feature—no missing steps, safe for user data, visible in CVGenie’s UI/dashboard, with clean error handling and ready for Replit deployment.