ERROR:
text
TypeError [ERR_INVALID_MODULE_SPECIFIER]: Invalid module "@db" is not a valid package name imported from /home/runner/workspace/server/routes.ts
ROOT CAUSE:
The TypeScript path alias @db (likely defined in tsconfig.json) is not being resolved at runtime by tsx. This happens when:

The tsconfig.json has path mappings like "@db": ["./server/db"] but tsx cannot resolve them

OR the import was accidentally changed from a relative path to an alias during the Gemini migration

FIX INSTRUCTIONS:
Step 1: Check server/routes.ts for Invalid Import
Open /server/routes.ts

Find any line that imports @db, for example:

ts
import { storage } from "@db";
Replace with the correct relative path:

ts
import { storage } from "./storage";
// OR
import { storage } from "./db/index";
Make sure ALL imports in server/routes.ts use relative paths (e.g., "./storage", "./db") NOT aliases like "@db" or "@/db"

Step 2: Check ALL Other Server Files
Search the entire /server directory for:

import ... from "@db"

import ... from "@/db"

Any other @ imports that are NOT @types/...

Replace ALL with correct relative paths based on actual file structure

Step 3: Verify tsconfig.json Path Mappings
Open tsconfig.json

If it has a "paths" section like:

json
"paths": {
  "@db": ["./server/db"],
  "@/*": ["./src/*"]
}
These should work for TypeScript compilation but tsx runtime does NOT support them by default

Solution: Either:

Remove path aliases entirely and use relative imports everywhere (RECOMMENDED for simplicity), OR

Add tsx resolver config (more complex)

Step 4: Rebuild and Test
After fixing all imports, run:

bash
npm run dev
Ensure the app starts without module resolution errors

EXAMPLE FIX:
BEFORE (BROKEN):

ts
// server/routes.ts
import { storage } from "@db";
AFTER (FIXED):

ts
// server/routes.ts
import { storage } from "./storage";  // Adjust based on actual file location
QUICK CHECKLIST:
 Find and replace ALL @db imports with relative paths in /server

 Find and replace ALL other @/* imports with relative paths

 Verify the app starts with npm run dev

 Confirm all routes and API endpoints work as before