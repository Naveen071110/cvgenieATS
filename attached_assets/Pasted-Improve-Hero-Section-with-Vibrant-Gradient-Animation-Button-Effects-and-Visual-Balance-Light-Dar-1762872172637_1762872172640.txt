Improve Hero Section with Vibrant Gradient, Animation, Button Effects, and Visual Balance (Light/Dark Mode, No Risk to Existing Code)

Hero Section Updates Required:

Vibrant Gradient on “Remote Work” Text

Make the gradient more vibrant and saturated (e.g., stronger blue-purple, teal-violet, or other brand colors).

Use a CSS gradient and ensure it looks great in both light and dark modes. Use CSS variables for the gradient so color values adapt to theme.

Animated Hero Heading

Add a fade-in or slight upward slide animation to the main hero heading. Animation should trigger on page load or section mount, using opacity, transform: translateY, and ease timing (e.g., 600ms).

The animation must be smooth and should not interfere with accessibility. Ensure animation runs once and does not loop.

Prominent “Grant My Wish” Button Hover Effect

Make the hover effect stand out:

Slight scale up (transform: scale(1.05)).

Brighter gradient or glowing box-shadow on hover.

Smooth transition (transition: all 0.3s ease).

For focus/keyboard navigation, ensure there’s a visible outline and color change.

All changes must be defined for both themes (light/dark).

Dynamic Background Animation/Particle Effect

Add a subtle animated geometric particle effect or low-opacity background animation to the hero section background.

Use a prebuilt React/CSS particle library (like tsParticles, or simple SVG/CSS patterns) for performance.

Ensure the animation is subtle, non-distracting, and does not impact loading speed.

Test visibility in both light and dark modes, using theme variables for particle colors.

Hero Image or Illustration on Right Side

Add a hero image or SVG illustration (representing people working remotely, productivity, or your app’s theme) to the right side of the hero section.

The image should balance with the left aligned text and button for visual symmetry.

Use an illustration style that matches your brand (not stocky, not generic).

For dark mode, use a version of the image with lighter accent colors; for light mode, a version with enough contrast.

Ensure the image is responsive and does not interfere with mobile layout or overlap the content.

Light/Dark Mode Consistency

Use CSS variables, theme classes, or a theme provider so gradients, animations, effects, and images consistently adapt based on current color mode.

Do not override or remove existing color logic that would break other sections.

Preview all final changes in both light and dark modes.

Safety

Do not modify or remove any previous code that impacts navigation, page layout, or unrelated sections.

Restrict all new code to the hero section and helper styles/components only.

Test all changes for accessibility and responsiveness across devices.

Summary of Required Result:
The hero section should be visually vibrant, animated, and dynamic in both light and dark modes, with safe new code that cannot break existing styles or features.